# 天枢三爻接口规范 (Tianshu Trigram Interface Specification)

> 版本: 1.0 | 日期: 2026-08-04 | 状态: 接口定义阶段
>
> 天枢不是 Agent 框架。天枢是信息、能量、物质三条流的调度系统。
> 城市大脑和工业物联网是同一套架构在不同场景下的实例化。
>
> 配套文档: [TIANSHU_WORLD_MODEL.md](TIANSHU_WORLD_MODEL.md) — 四层世界模型（不可观·可观·可测·可控）

---

## 一、三爻的正式定义

三爻不是三个代码目录。三道不能互相绕过的闸门：

```
                    ┌─────────────────────────┐
                    │   天 (TIAN)  Governance  │
                    │   规则 · 仲裁 · 时间协调   │
                    │   权限: OVERRULE          │
                    │   时间: 战略级(小时~年)     │
                    └──────────┬──────────────┘
                               │ OVERRIDE ↓    ↑ 升级
                    ┌──────────┴──────────────┐
                    │   人 (REN)  Decision     │
                    │   意图 · 优先级 · 调度     │
                    │   权限: MANAGE            │
                    │   时间: 运营级(分钟~小时)   │
                    └──────────┬──────────────┘
                               │ 指令 ↓      ↑ 报告
                    ┌──────────┴──────────────┐
                    │   地 (DI)   Execution    │
                    │   感知 · 执行 · 物理交互   │
                    │   权限: EXECUTE           │
                    │   时间: 战术级(毫秒~分钟)   │
                    └─────────────────────────┘
```

### 1.1 天 (TIAN) — Governance 治理层

**职责**：定义"能不能做"的边界。规则引擎 + 冲突仲裁 + 跨时间尺度信息协调。

- 不直接执行任何物理操作——只裁定权限
- 拥有 OVERRIDE 权限——可以否决下层任何决策
- 负责信息时效性仲裁：当两个不同时间尺度的 Agent 对同一实体给出矛盾信息时，裁定信任哪一个
- 资源配额管理：token 预算、API 调用次数、计算资源分配

**典型 Agent**：审计员、合规检查器、安全约束引擎、资源配额管理器

**不负责**：理解用户意图（那是人层的活）、执行具体操作（那是地层的活）

### 1.2 人 (REN) — Decision 决策层

**职责**：理解意图、设定优先级、调度 Agent、确认不可逆决策。

- 接收天层的规则约束，向下调度地层的执行
- 拥有 MANAGE 权限——可以调度地层的 Agent 并行或串行工作
- 不可逆决策（如删除文件、发起交易、启动安全制动）必须得到人层确认
- 人层是唯一可以与用户直接对话的层——天层和地层通过人层中转

**典型 Agent**：规划器、调度器、风险评估器、谈判 Agent、优先级排序器

**不负责**：定义规则（天层）、执行工具调用（地层）

### 1.3 地 (DI) — Execution 执行层

**职责**：把物理世界的信息带进来，把决策输出到物理世界。

- 传感器→数据流：温度探头、数据库查询、网页抓取、API 调用
- 执行器→命令流：信号灯控制、文件写入、shell 执行、设备启停
- 只能在赋予的权限边界内执行——超界的操作在天层被拦截

**典型 Agent**：搜索器、浏览器、Shell 执行器、传感器读取器、设备控制器

**不负责**：判断该不该做（人层和天层）、理解为什么做（人层）

### 1.4 层间路由规则

```
允许:  地→人→天  (向上报告/升级)
       天→人→地  (向下指令/否决)
       同层广播   (Agent间协作)

禁止:  地↔天      (必须经过人层——不可跨层直连)
```

---

## 二、跨层消息协议 (Cross-Layer Message Protocol)

### 2.1 消息格式

每一条消息都是可审计的。消息本身幂等——同一个 `decision_id` 重复发送不产生副作用。

```json
{
  "decision_id": "d_a1b2c3d4e5f6",
  "timestamp":   1722768000000,
  "ttl_ms":      60000,
  "source":      {"layer": "di", "agent_id": "temp_sensor", "instance_id": "workshop_3"},
  "target":      {"layer": "ren", "agent_id": "production_scheduler"},
  "intent":      "3号车间温度超过安全阈值，当前85°C",
  "payload": {
    "location":   "workshop_3",
    "temperature": 85.0,
    "threshold":   75.0,
    "confidence":  0.98
  },
  "constraints": {
    "time_budget_ms":   5000,
    "permission_level":  0
  },
  "priority":    "URGENT",
  "direction":   "up",
  "audit_trail": [
    {"decision_id": "d_orig_001", "layer": "di", "action": "detected_anomaly", "ts": 1722767990000}
  ]
}
```

### 2.2 消息优先级

| 级别 | 含义 | 谁能发 | 示例 |
|------|------|--------|------|
| NORMAL | 常规通信 | 所有层 | 定时报告、状态更新、查询 |
| URGENT | 需要立即响应 | 人层、天层 | 异常检测、安全告警、超时预警 |
| OVERRIDE | 否决/强制制动 | **仅天层** | 否决违规决策、紧急停止 |

### 2.3 消息约束 (MessageConstraints)

每条消息携带硬约束——接收方必须遵守：

- `time_budget_ms`: 处理此消息的时间预算（0=无限制）
- `permission_level`: 执行时允许的最高操作级别
- `resource_limits`: 具体资源限制（token 数、工具调用次数、网络权限等）

---

## 三、Agent 时间尺度模型

### 3.1 核心问题

**所有现有多 Agent 框架（ChatDev、MetaGPT、AutoGen、CAMEL）共享一个未被声明的假设：所有 Agent 在同一时刻运行，状态同步即时完成。**

在真实系统中——无论是城市大脑还是工业物联网——这个假设崩溃了：

```
产线传感器:  100ms采样一次
质检分析:    1小时产出一份抽检报告
排产调度:    分钟级调整产线分配
供应链规划:  每天做一次采购决策

当你做供应链决策时，产线数据可能已经是数小时前的。
这不是 bug——这是系统的结构特征。
```

### 3.2 时间尺度声明

每个 Agent 注册时必须声明：

```python
TimeScale(
    tick_ms=100,                          # 观察/行动间隔(ms)
    decay=InfoDecayConfig(
        half_life_ms=5_000,               # 5秒后半衰
        base_confidence=1.0,
        min_confidence=0.1,
    ),
    sync=SyncMode.PUSH,                   # 变化时广播
    max_staleness_ms=0,                   # 不允许陈旧数据
)
```

### 3.3 同步模式

| 模式 | 行为 | 适用场景 |
|------|------|----------|
| PUSH | 状态变化时主动广播 | 高频传感器（振动探头、交通信号） |
| PULL | 按需轮询 | 低频任务（供应链规划、城市规划） |
| THRESHOLD | 变化超阈值才通知 | 大数据量（电网负载、质检分析） |

### 3.4 信息时效衰减函数

```
confidence(t) = base × e^(-t / half_life × ln(2))
```

| 数据来源 | half_life | 说明 |
|----------|-----------|------|
| 振动传感器 | 5 秒 | 产线实时性要求 |
| 卫星图像 | 1 小时 | 过顶周期决定 |
| 质检抽检 | 3 小时 | 批次生产周期 |
| 日报数据 | 24 小时 | 报告延迟 + 人为判断 |
| 人口普查 | 1 年 | 城市大脑战略数据 |

### 3.5 天层的跨时间仲裁

当两个 Agent 对同一实体给出矛盾信息时，天层根据以下规则仲裁：

1. 分别计算两个信息源在**当前时刻**的置信度（基于衰减函数）
2. 比较置信度——置信度高的优先
3. 如果置信度相近（差值<0.1），比较信息源的**历史准确率**
4. 如果仍然无法裁决，标记为冲突并升级到人层确认

这套机制在现有 Agent 框架中**完全缺失**。

---

## 四、天曜审计标准

### 4.1 审计不是事后记录

天曜的核心理念：**审计是在决策前就写清楚"为什么"。** 事后回溯只是审计的第二个功能。

### 4.2 六问标准

一条合格的审计记录必须能回答六个问题：

| # | 问题 | 字段 | 示例 |
|---|------|------|------|
| 1 | 谁做的决策？ | agent_ref | `di:temp_sensor#workshop_3` |
| 2 | 当时有什么信息可用？ | available_info + info_snapshot_hash | "传感器数据(t=1000,置信度0.9)" |
| 3 | 考虑了哪些替代方案？ | alternatives_considered | ["紧急停机", "降功率运行", "持续监测"] |
| 4 | 什么约束在生效？ | active_rules | ["SAFETY-3: 高温区域人员禁入"] |
| 5 | 结果如何？ | decision_made + outcome | "降功率运行" + "温度已恢复安全范围" |
| 6 | 能复现吗？ | replay_trace | 确定性事件序列的哈希链 |

### 4.3 审计完备度四级

| 级别 | 记录内容 | 何时使用 |
|------|----------|----------|
| L1 BASIC | 决策ID + 时间戳 + 结果 | 常规对话、只读查询 |
| L2 SNAPSHOT | + 系统状态快照 | 工具调用、文件操作 |
| L3 FULL | + 推理链 + 替代方案 | 写入操作、资源分配 |
| L4 EVALUATED | + 事后评估 | 不可逆决策、安全关键操作 |

### 4.4 三个审计场景

**运行时**：在 `AgentCore.run_stream()` 中，每次工具调用前 `AuditSixQuestions.record()` 被调用，生成一条快照。

**回溯时**：用户通过 `/audit <decision_id>` 查看完整决策链——从最初的用户输入到最终的工具执行，每一环都能看到"谁做了什么、为什么"。

**对抗性评估时**：不测"成功率"，而测"失效模式"——在什么时间压力/信息缺失/信息冲突条件下，系统做出了错误决策？审计链就是评估数据源。

---

## 五、场景实例化

### 5.1 城市大脑

```
时间轴线: 秒 ───────────────→ 年

交通信号Agent (地·PUSH·1s)
    ↓ 车流量数据
交通优化器  (人·PUSH·30s)
    ↓ 信号配时方案
资源治理器  (天·THRESHOLD·1h)
    ── 跨区域资源仲裁

电网监控    (地·PUSH·60s)
    ↓ 负载数据
负载均衡器  (人·PUSH·5min)
    ↓ 调度指令

城市规划师  (人·PULL·1d)
    ↓ 用地评估
资源治理器  (天·THRESHOLD·1h)
    ── 配额冲突裁决
```

### 5.2 工业物联网

```
时间轴线: 毫秒 ──────────────→ 天

振动传感器  (地·PUSH·100ms)
    ↓ 振动数据
排产调度器  (人·PUSH·1min)
    ↓ 产线调整方案
安全守门人  (天·PUSH·1s·OVERRIDE)
    ── 安全约束实时校验

质检分析    (地·THRESHOLD·1h)
    ↓ 抽检报告
排产调度器  (人·PULL·30min)
    ↓ 批次计划调整
供应链规划  (人·PULL·1d)
    ↓ 采购决策
安全守门人  (天·PUSH·1s)
    ── 违规操作紧急制动
```

### 5.3 差异只在配置

两个场景共享同一套代码——`trigram.py` 中的 `TrigramMessage`、`TimeScale`、`AuditSixQuestions` 完全相同。差异仅在 `AgentRegistration` 的参数：

```python
# 城市
traffic_sensor = AgentRegistration(
    ref=AgentRef(Layer.DI, "traffic_sensor"),
    time_scale=TimeScale(tick_ms=1000, decay=InfoDecayConfig(30_000)),
    ...
)

# 工业
vibration_sensor = AgentRegistration(
    ref=AgentRef(Layer.DI, "vibration_sensor"),
    time_scale=TimeScale(tick_ms=100, decay=InfoDecayConfig(5_000)),
    ...
)
```

---

## 六、实现路线图

本规范定义了**接口**——天·人·地三层之间说了什么、怎么说的。后续实施分为三个阶段：

### Phase A: 接口层落地（当前阶段）
- [x] `trigram.py` — 数据模型
- [x] `TIANSHU_INTERFACE_SPEC.md` — 本文档
- [x] SDK `__init__.py` 导出 trigram 类型

### Phase B: 运行时接入
- [ ] `AgentCore` 消息通道改造——工具调用走 `TrigramMessage`
- [ ] 天曜审计接入 `AuditSixQuestions`
- [ ] 策略引擎对接 `MessageConstraints`

### Phase C: 时间尺度运行时
- [ ] Agent 注册时声明 `TimeScale`
- [ ] 天层实现跨时间尺度信息仲裁
- [ ] 信息衰减函数注入到消息路由

---

## 七、关键设计原则

1. **三爻是闸门不是目录。** 消息必须经过三层校验，不可绕过。
2. **时间是第一公民。** 每条消息有有效期，每个 Agent 有时间尺度。这不是事后优化的细节——是架构的骨架。
3. **审计不是事后记录。** 决策前就写清楚"为什么"。审计链就是系统的记忆。
4. **场景共享同一套接口。** 差异只在 Agent 配置——tick 频率、衰减函数、权限级别。不写两套代码。
5. **OVERRIDE 只能来自天层。** 这是不可违反的硬约束。任何试图从地/人层发 OVERRIDE 的行为都被 `validate_message()` 拦截。
