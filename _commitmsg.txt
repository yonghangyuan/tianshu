docs: TS-018 MC1 完成 — 手机助手最小对话闭环（模拟器验收）

MC1 交付（F:\tianshu_dev\android，独立工程）:
- agent.py: Agent 循环手机化移植（cli_proto → Chaquopy）——
  回调驱动替代 input()，事件流 on_event(turn/tool/tool_result/denied)，
  三级闸门 REQUIRED 级走 confirm_cb 确认卡，max_iterations=20
- provider.py: DS API requests 版（重试/限流/超时分类错误）
- tools.py: 6 手机工具 schema（plan 模式只读过滤）+ ApprovalLevel 闸门
- kotlin_bridge.py: Chaquopy 回调粘合（Kotlin 对象注入 Python 属性）
- AgentBridge.kt: 单例桥（config→Agent 重建/事件转发/确认阻塞）
- ChatActivity.kt: 零 XML 代码布局聊天 UI——消息流/模式切换(循环按钮)/
  重置/设置(AlertDialog)/确认对话框(CountDownLatch 阻塞)
- LoginActivity 退役为跳板（v2 无服务器登录）

验收（模拟器 Medium_Phone_API_36.1 真跑）:
  用户: reply with exactly:smc1-ok
  助手: smc1-ok  ← DS API 真实回复
  状态: Token: 934 (↑904 ↓30)
链路: Kotlin UI → AgentBridge → Python Agent → DS API → 渲染

联调坑备案:
- adb input text 空格截断 → %s 转义；键盘弹出致坐标漂移 → keyevent 111 收起再点
- Git Bash 吞 /sdcard 路径 → //sdcard 双斜杠
- debug 包注入配置: adb push + run-as cp shared_prefs（UI 自动化填 key 不可靠）
- uiautomator dump 必须每次重 dump（focused/bounds 随键盘变）

下一步 MC2: exec_tool 桥接 AgentService（手机工具闭环），
验收"调亮度到最低"。

Co-Authored-By: jiojio <16404193+jiojio21@user.noreply.gitee.com>
Co-Authored-By: DeepSeek <noreply@deepseek.com>
Co-Authored-By: Claude <noreply@anthropic.com>
